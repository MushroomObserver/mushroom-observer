<%
  @title = :species_list_create_title.t

  tabs = [
    link_to(:name_lister_title.t, action: :name_lister)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<%= form_tag(action: :new, approved_where: @place_name) do %>
  <%= render(partial: "form_species_lists", locals: {button: :CREATE}) %>
<% end %>
