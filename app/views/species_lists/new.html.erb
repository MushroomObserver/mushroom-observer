<%
  @title = :species_list_create_title.t

  tabs = [
    link_to(:name_lister_title.t, new_species_list_name_lister)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
  @container = :wide
%>

<%= render(partial: "form", locals: { button: :CREATE }) %>
