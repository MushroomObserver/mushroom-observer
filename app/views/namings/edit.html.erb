<%
  @observation = @params.observation
  @reason = @params.reason
  @title = :edit_naming_title.t(id: @observation.id)

  # tabs = [
  #   link_with_query(:cancel_and_show.t(type: :observation),
  #                   observation_path(@observation))
  # ]
  # @tabsets = { right: draw_tab_set(tabs) }
  # TODO: Is this really the right navbar title/anchor?
  # Should it be Observations?
  @navbar = {} # reset
  @navbar[:title] = { title: :NAMINGS.t, url: edit_naming_path }
  @navbar[:links] = [
    { title: :cancel_and_show.t(type: :observation),
      url: observation_path(@observation.id, q: get_query_param),
      icon: "fa-backspace" }
  ]
%>

<%= tag.div class: "form-max-width mt-4" do %>
  <%= tag.h1 @title.humanize, class: "display-1" %>
  <%= render "form", locals: { action: :edit, button_name: :SAVE_EDITS.l,
                               show_reasons: true } %>
<% end %>
