<%
  @title = :image_edit_title.t(name: @image.unique_format_name)

  tabs = [
    link_with_query(:cancel_and_show.t(type: :image), image_path(@image))
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<% render "images/form" locals: { action: :edit,
                                  button_name: :SAVE_EDITS.l,
                                  leave_out_original_file_name: false } %>
