<%
  @title = :edit_classification_title.t(name: @name.display_name)

  tabs = [
    link_with_query(:cancel_and_show.t(type: :name), @name.show_link_args)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
  action = add_query_param(edit_name_classification_path(@name.id))
%>

<div class="mt-3 container-text">

  <%= form_with(action: action) do |f| %>

    <%= f.label(:classification, :"form_names_classification".t + ":") %>
    <p class="help-block mt-0">
      <%= rank = rank_as_lower_string(@name.rank)
      :form_names_classification_help.l(rank: rank) %>
    </p>
    <%= f.text_area(:classification, @name.classification,
                      rows: 10, class: "form-control",
                      data: { autofocus: true }) %>

    <%= f.submit(:SAVE.t, class: "btn btn-default center-block mt-3") %>

  <% end %>

</div><!--.container-text-->
