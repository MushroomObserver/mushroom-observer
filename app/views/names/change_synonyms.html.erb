<%
  @title = :name_change_synonyms_title.t(name: @name.display_name)

  tabs = [
    link_to(:cancel_and_show.t(type: :name),
            name_path(@name.id, q: get_query_param)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<%= render "names/form_synonyms" %>
