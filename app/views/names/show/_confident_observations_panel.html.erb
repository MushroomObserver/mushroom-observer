<% if @first_four&.length&.positive? %>
  <%= card_block(
    header: tag.h6("#{:show_name_most_confident.t}:"),
    id: "name_confident_observations"
  ) do %>
    <% @first_four.each do |obs| %>
      <%= tag.div(class: "text-center my-4") do
        show_best_image(obs)
      end %>
    <% end %>
  <% end %>
<% end %>
