<%
header = [tag.h6(:show_name_notes.t)]
if @user
  header << tag.div do
              edit_button(
                name: :EDIT.t,
                target: add_query_param(edit_name_path(@name.id))
              )
            end
end
header = header.safe_join
%>

<%= card_block(id: "name_notes", header: header) do @name.notes.tpl end %>
