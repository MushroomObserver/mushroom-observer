<%
header = [
  tag.h6(:show_name_classification.t),
  tag.div do
    link_to(:EDIT.t,
            add_query_param(edit_name_classification_path(@name.id)))
  end
].safe_join
%>

<%= tag.div(id: "name_classification") do
  card_block(id: "classification_section",
             header: header, inner_class: "list-indented") do
    render(partial: "names/show/classification")
  end
end %>
