<%
  @title ||= :create_name_description_title.t(name: @name.display_name)

  tabs = [
    link_with_query(:cancel_and_show.t(type: :name),
                    action: :show,
                    id: @description.name_id)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<%= render "form", locals: { button_name: :CREATE.l } %>
