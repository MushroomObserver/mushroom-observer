<%
  @title ||= :create_name_description_title.t(name: @name.display_name)

  tabs = [
    link_with_query(:cancel_and_show.t(type: :name),
                    name_path(@description.name_id))
  ]
  @tabsets = { right: draw_tab_set(tabs) }

  action = name_descriptions_path(@description.name_id, q: get_query_param)
%>

<%= render(partial: "form",
           locals: { action: action, button: :CREATE }) %>
