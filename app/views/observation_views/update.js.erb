// What to do when receiving a request to mark obs as reviewed/not

var label = $("#mark_as_reviewed_<%= j @obs_id %>").parent();
var form = button.parent();

if(<%= j @reviewed %> == 1) {
  label.text("<%= j :marked_as_reviewed.t %>").addClass("active");
} else {
  label.text("<%= j :mark_as_reviewed.t %>").removeClass("active");
}
