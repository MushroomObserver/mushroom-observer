<%
  @title = :sequence_add_title.t
  tabs = [
    link_with_query(:cancel_and_show.t(type: :observation),
                    @observation.show_link_args)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<%= render "observation_title", locals: { observation: @observation } %>

<%= render "form" %>
