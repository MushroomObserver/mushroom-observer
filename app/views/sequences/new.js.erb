$('body').append('<%= j render(
  partial: "shared/modal_form",
  locals: {
    identifier: "sequence",
    title: :sequence_add_title.l,
    form: "sequences/form",
    action: :create
  }
) %>');

<%= render(partial: "shared/modal_form_bindings", format: :js,
           locals: { identifier: "sequence" }) %>
