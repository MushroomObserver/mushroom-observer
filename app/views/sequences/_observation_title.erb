<%# Observation title for add/edit/show_sequence pages %>

<%= tag.div(class: "mt-3") do
  [tag.strong("#{:OBSERVATION.t}:"),
   observation.name.display_name.t,
   "(#{observation.id})"].safe_join(" ")
end %>
