<%
  @title = :edit_visual_group_title.t(name: @visual_group.name)
%>

<%= render 'form', visual_group: @visual_group %>

<%= link_to 'Show', @visual_group %> |
<%= link_to 'Back', visual_model_visual_groups_path(@visual_group.visual_model) %>
