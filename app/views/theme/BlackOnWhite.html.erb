<%
  @title = :theme_black_on_white.tl

  tabs = [
    link_to(:theme_list.t, theme_color_themes_path),
    link_to(:app_preferences.t, edit_account_preferences_path)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<%= :theme_black_on_white_description.tp(link: "XXX").sub("XXX", link_to("**__Amanita__**".t, action: :Amanita)).html_safe %>

<%= :theme_switch.tp(theme: :BlackOnWhite.t) %>
