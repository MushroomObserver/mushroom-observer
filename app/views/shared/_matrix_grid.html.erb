<% #passing in the objects as a collection with a partial is much faster, but not always possible if we ant to render a block %>
<% unless defined? object_counter %>
  <% objects.each_with_index do |object, idx| %>
    <%= yield(object) %>
  <% end %>
<% else %>
  <%= yield(object) %>
<% end %>
