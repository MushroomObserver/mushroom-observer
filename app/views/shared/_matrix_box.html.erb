<%
   # Requires local variable object: RssLog, Observation, Image, etc. instance.
   presenter = RssLogPresenter.new(object, @view)
%>

<% if presenter %>
<li class="col mb-4">
  <div class="card rss-box-details h-100">
    <div class="card-img-top">
      <%= presenter.thumbnail %>
    </div><!-- .card-img-top -->
    <div class="card-body p-3 lh-2">
      <p class="card-title rss-what h5 mb-2">
        <%= presenter.what %>
      </p><!-- .rss-what -->
      <p class="card-text rss-where mb-2 small">
        <%= presenter.where %>
      </p><!-- .rss-where -->
      <p class="card-text rss-what mb-0 small">
        <% unless presenter.when.blank? %>
          <%= presenter.when %>: <%= presenter.who %>
        <% end %>
      </p><!-- .rss-what -->
    </div><!-- .card-body -->
    <div class="card-footer bg-transparent px-3 text-muted">
      <p class="rss-detail mb-0 small">
        <%= presenter.detail %>
      </p><!-- .rss-detail -->
      <p class="rss-what mb-0 small">
        <%= presenter.fancy_time %>
      </p><!-- .rss-what -->
    </div><!-- .card-footer -->
  </div><!-- .card -->
</li>
<% end %>
