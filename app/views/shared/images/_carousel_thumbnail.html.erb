
<%
  presenter_args = local_assigns.except(:image).merge({ fit: :contain })
  presenter = ImagePresenter.new(image, @view, presenter_args)
%>


<%= image_tag(presenter.img_src, presenter.options_lazy) %>
