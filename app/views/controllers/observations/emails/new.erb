<%
add_page_title(:ask_observation_question_title.t(
  name: @observation.unique_format_name
))
%>

<%= render(Components::ObserverQuestionForm.new(
      FormObject::ObserverQuestion.new,
      observation: @observation,
      local: true
    )) %>
