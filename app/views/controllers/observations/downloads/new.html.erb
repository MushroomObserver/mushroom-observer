<%
# The form partial is also called by species_lists/downloads
add_page_title(:download_observations_title.t)

# add_context_nav(observation_download_tabs)
%>

<%= render(Components::ObservationsDownloadForm.new(
      query_param: q_param
    )) %>
