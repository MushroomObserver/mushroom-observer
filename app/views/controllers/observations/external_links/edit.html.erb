<%
container_class(:full)
add_edit_title(:EXTERNAL_LINK.l, @external_link)
# add_context_nav(
#   external_link_form_edit_tabs(back: @back, link: @external_link,
#                                obj: @back_object)
# )
%>

<div class="row">
  <div class="col-xs-12 col-sm-7">
    <%= render(Components::ExternalLinkForm.new(
          @external_link,
          observation: @observation,
          sites: [@site],
          site: @site,
          base_urls: @base_urls,
          user: @user,
          back: @back
        )) %>
  </div>

  <div class="col-xs-12 col-sm-5">
    <ul class="row list-unstyled">
      <%= render(::Components::MatrixBox.new(
                   user: @user,
                   object: @external_link.observation,
                   columns: "col-xs-12")) %>
    </ul>
  </div>
</div>
<!--.row-->
