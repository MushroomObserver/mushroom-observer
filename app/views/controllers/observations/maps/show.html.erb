<%
container_class(:full)
add_page_title(:map_observation_title.t(id: @observation.id))
add_context_nav(observation_maps_tabs(query: @query))
%>

<%= make_map(objects: @observations, # it's an array, just one MinimalMapObs
             query_params: query_params(@query), zoom: 2,
             map_type: "observation") %>
