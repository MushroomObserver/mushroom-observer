<%
@container = :full

add_page_title(:map_locations_title.t(
  locations: map_join_and_truncate(@query, :locations)
))
add_tab_set(observation_maps_tabs(query: @query))

objects = @observations
objects += @locations if @locations.any?
%>

<%= make_map(objects: objects, # CollapsibleCollection
             query_params: query_params(@query), zoom: 2, map_type: "info") %>
