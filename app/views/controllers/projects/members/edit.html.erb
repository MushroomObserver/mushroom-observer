<%
add_page_title(:change_member_status_title.t(
  name: @candidate.legal_name,
  title: @project.title
))
add_context_nav(project_member_form_edit_tabs(project: @project))
%>

<%= render(partial: "projects/members/form",
           locals: { project: @project, candidate: @candidate }) %>

<%= render(partial: "projects/groups", locals: { project: @project }) %>
