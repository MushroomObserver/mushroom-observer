<%
container_class(:wide)
add_project_banner(@project)
%>

<% if @project.is_admin?(@user) %>
  <%= render(partial: "projects/target_locations/widget",
             locals: { project: @project }) %>
<% end %>

<%= render(partial: "projects/locations/locations_table",
           locals: { project: @project, locations: @locations,
                     user: @user }) %>
