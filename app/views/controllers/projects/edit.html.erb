<%
add_page_title(:edit_project_title.t(title: @project.title))
add_context_nav(project_form_edit_tabs(project: @project))

button = :SAVE_EDITS.l
%>

<%= render(partial: "projects/form", locals: { button: button }) %>
