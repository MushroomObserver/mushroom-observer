<%
# No-JS fallback view for creating an API key. JS-enabled users hit
# the inline-collapse panel on the index page; users without JS land
# here via the link in `api_keys_new_button_container`.
add_page_title(:account_api_keys_title.t)
add_context_nav(Tab::Account::APIActions.new)
%>

<%= :account_api_keys_help.tp %>

<%= render(Views::Controllers::Account::APIKeys::Form.new(
             @key,
             action: account_api_keys_path,
             id: "new_api_key_form")) %>
