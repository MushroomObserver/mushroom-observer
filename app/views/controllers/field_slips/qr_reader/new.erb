<%=
form_with(url: { action: :create }, data: { controller: "qr-reader" } ) do |f|
  text_field_with_label(form: f, field: :qr_code, label: "QR text",
                        data: { qr_reader_target: "input",
                                action: "qr-reader#handleInput" })
end
%>
