<%
add_page_title("#{:FIELD_SLIP.t}: #{@field_slip.code}")
add_edit_icons(@field_slip)
container_class(:wide)
%>

<%= render(Components::Alert.new(message: notice, level: :success)) if notice %>

<%= content_padded { render(@field_slip) } %>

<%= render Components::ObjectFooter.new(user: @user, obj: @field_slip) %>
