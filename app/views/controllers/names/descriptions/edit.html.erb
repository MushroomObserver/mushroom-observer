<%
add_page_title(:edit_name_description_title.t(name: @description.format_name))
add_context_nav(Tab::NameDescription::FormEdit.new(
  description: @description,
  admin: @description.is_admin?(@user) || in_admin_mode?
))
%>

<%= render(Views::Controllers::Descriptions::Form.new(
             @description,
             licenses: @licenses,
             user: @user,
             merge_opts: { merge: @merge, old_desc_id: @old_desc_id,
                           delete_after: @delete_after }
           )) %>
