<%
add_page_title(:edit_name_description_title.t(name: @description.format_name))
add_context_nav(name_description_form_edit_tabs(description: @description,
                                                user: @user))
%>

<%= render(Components::DescriptionForm.new(
             @description,
             licenses: @licenses,
             user: @user,
             merge_opts: { merge: @merge, old_desc_id: @old_desc_id,
                           delete_after: @delete_after }
           )) %>
