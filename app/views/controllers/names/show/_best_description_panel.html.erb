<%
heading = :show_name_general_description.l
link1 = icon_link_with_query(
  :show_name_see_more.l, name_description_path(@name.description.id),
  icon: :list
)
link2 = icon_link_with_query(
  :EDIT.l, edit_name_description_path(@name.description.id), icon: :edit
)
heading_links = @user ? [link1, link2].safe_join(" | ") : ""
%>

<%= panel_block(id: "name_general_description",
                heading: heading, heading_links: heading_links) do
  @best_description.tpl
end %>
