<%
heading = :show_name_general_description.l
link1 = link_with_query(:show_name_see_more.l,
                        name_description_path(@name.description.id))
link2 = link_with_query(:EDIT.l,
                        edit_name_description_path(@name.description.id))
heading += " [ #{link1} | #{link2} ]".html_safe
%>

<%= panel_block(id: "name_general_description", heading: heading) do
  @best_description.tpl
end %>
