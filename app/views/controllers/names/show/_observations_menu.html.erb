<%#  This is just a two-column panel %>
<%
heading = :about_this_taxon.l
tracker_tab = if NameTracker.find_by(name_id: @name.id, user_id: @user.id)
                Tab::Name::EditTracker.new(name: @name)
              else
                Tab::Name::NewTracker.new(name: @name)
              end
heading_link = render(Components::IconLink.new(tab: tracker_tab))
%>

<%= render(Components::Panel.new(
      panel_id: "name_observations_menu"
    )) do |panel| %>
  <%= panel.with_heading { heading } %>
  <%= panel.with_heading_links { heading_link } %>
  <%= panel.with_body do %>
    <%= tag.div(class: "row") do

      concat(tag.div(class: "col-sm-6 name-section") do
        concat(tag.p(:show_observations_of.t))
        concat(tag.div(class: "pl-3") do
          concat(name_related_taxa_observation_links(@name, @obss))
          # Observations of this name's subtaxa, if any
          concat(tag.p do
            [link_to(:show_subtaxa_obss.l,
                    add_q_param(observations_path, @subtaxa_query)),
            " (#{@has_subtaxa})"].safe_join
          end) if @has_subtaxa
        end)
        concat(tag.div(class: "py-3") do
          concat(tag.p(link_to(*Tab::Name::OccurrenceMap.new(name: @name).to_a)))
        end)
      end)

      concat(tag.div(class: "col-sm-6 name-section") do
        concat(tag.p("#{:research_links.l}:"))
        concat(tag.div(class: "pl-3") do
          if @name.classification =~ /Phylum: _Ascomycota_/
            concat(tag.p(link_to(
              *Tab::Name::AscomyceteOrg.new(name: @name).to_a
            )))
          end
          if @name.eol_url
            concat(tag.p(link_to(*Tab::Name::Eol.new(name: @name).to_a)))
          end
          concat(tag.p(link_to(*Tab::Name::Gbif.new(name: @name).to_a)))
          concat(tag.p(link_to(
            *Tab::Name::UserGoogleImages.new(name: @name, user: @user).to_a
          )))
          concat(tag.p(link_to(
            *Tab::Name::GoogleSearch.new(name: @name).to_a
          )))
          concat(tag.p(link_to(*Tab::Name::Inat.new(name: @name).to_a)))
          if @name.searchable_in_registry?
            concat(tag.p(link_to(
              *Tab::Name::MushroomExpert.new(name: @name).to_a
            )))
            concat(tag.p(link_to(*Tab::Name::Mycoportal.new(name: @name).to_a)))
          end
          concat(tag.p(link_to(
            *Tab::Name::NcbiNucleotide.new(name: @name).to_a
          )))
          concat(tag.p(link_to(*Tab::Name::Wikipedia.new(name: @name).to_a)))
        end)
      end)
    end %>
  <% end %>
<% end %>
