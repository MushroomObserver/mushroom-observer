<%= render(Components::Panel.new(
      inner_id: "name_projects",
      heading: :show_name_create_draft.t
    )) do %>
  <% @projects.each do |project| %>
    <%= link_to(
      project.title,
      new_name_description_path(@name.id, project: project.id, source: :project)
    ) %><%= safe_br %>
  <% end %>
<% end %>
