<%= tag.div("#{:app_observations_left.t}:", class: classes[:heading]) %>
<% sidebar_observations_tabs(@user).compact.each do |link| %>
  <%= sidebar_nav_link(link, class: classes[:indent]) %>
<% end %>
<%# active_link_to(:app_latest.t, root_path,
                   class: classes[:indent], id: "nav_observations_link") %>
<%# active_link_to(:app_create_observation.t, new_observation_path,
                   class: classes[:indent],
                   id: "nav_new_observation_link") if @user %>
<%# active_link_to(:app_your_observations.t,
                   observations_path(by_user: @user.id),
                   class: classes[:indent],
                   id: "nav_your_observations_link") if @user %>
<%# active_link_to(:app_help_id_obs.t, identify_observations_path,
                   class: classes[:indent],
                   id: "nav_identify_observations_link") %>
