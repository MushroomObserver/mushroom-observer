<%=
tag.div("#{:app_admin.t}:", class: classes[:heading])
%>
<% sidebar_admin_tabs.compact.each do |link| %>
  <%= sidebar_nav_link(link, class: classes[:admin]) %>
<% end %>
<%=
button_to(:app_turn_admin_off.t, admin_mode_path(turn_off: true),
          class: [classes[:admin], "btn btn-link"], id: "nav_admin_off_link",
          method: :post)
%>
