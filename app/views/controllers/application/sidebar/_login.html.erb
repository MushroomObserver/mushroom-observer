<%= tag.div("", class: classes[:heading]) do
  [
    tag.i("", class: "glyphicon glyphicon-user"),
    tag.span("#{:app_account.t}:")
  ].safe_join
end %>
<% sidebar_login_tabs.compact.each do |link| %>
  <%= sidebar_nav_link(link, class: classes[:indent]) %>
<% end %>
