<%=
tag.div("#{:INDEXES.t}:", class: classes[:heading])
%>
<% sidebar_indexes_tabs.compact.each do |link| %>
  <%= sidebar_nav_link(link, class: classes[:indent]) %>
<% end %>

<%#
[
active_link_to(:GLOSSARY.t, glossary_terms_path,
               class: classes[:indent], id: "nav_glossary_terms_link"),
active_link_to(:HERBARIA.t, herbaria_path,
               class: classes[:indent], id: "nav_herbaria_link"),
active_link_to(:LOCATIONS.t, locations_path,
               class: classes[:indent], id: "nav_locations_link"),
active_link_to(:NAMES.t, names_path(has_observations: true),
               class: classes[:indent], id: "nav_name_observations_link"),
active_link_to(:PROJECTS.t, projects_path(by: "updated_at"),
               class: classes[:indent], id: "nav_projects_link")
].safe_join
%>
