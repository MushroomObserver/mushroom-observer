<%#
Plain nav set. Takes `links` as an array of <a> or <button> links.
Just adds <small> and line breaks.
%>

<% links ||= [] %>

<%= context_nav_dropdown(title: "Actions", id: "context_nav_toggle", links:) %>
