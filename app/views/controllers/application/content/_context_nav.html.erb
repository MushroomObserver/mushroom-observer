<%#
Plain nav set. Takes `links` as an array of <a> or <button> links.
Just adds <small> and line breaks.
%>

<% links ||= [] %>
<%= tag.ul(class: "list-unstyled mb-0 small") do %>
  <% links.reject(&:nil?).each do |link| %>
    <%= tag.li(link) %>
  <% end %>
<% end %>
