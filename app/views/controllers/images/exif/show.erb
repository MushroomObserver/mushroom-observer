<%
add_page_title(:exif_data_for_image.t(image: @image.id))
add_context_nav(images_exif_show_tabs(image: @image))
set_container(:text)
%>

<%=
if @status
  render(partial: "images/exif/data", locals: { data: @data })
else
  render(plain: @result, status: :internal_server_error)
end
%>
