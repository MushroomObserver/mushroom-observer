<%
container_class(:full)
add_index_title(@query, map: true)
add_context_nav(location_map_tabs(query: @query))
%>

<%= make_map(
  objects: @locations, # it's an array, but just one MinimalMapLocation
  query_param: q_param(@query), zoom: 2, map_type: "location"
) %>
