<% if @description&.notes? %>

  <%
  heading = :show_name_general_description.l
  link1 = icon_link_to(*location_show_description_tab(@location))
  link2 = icon_link_to(*location_edit_description_tab(@location))
  heading_links = @user ? [link1, link2].reject(&:nil?).safe_join(" | ") : ""
  %>

  <%= render(Components::Panel.new(
        panel_id: "location_general_description"
      )) do |panel| %>
    <%= panel.with_heading do %>
      <%= heading %>
    <% end %>
    <% if @user %>
      <%= panel.with_heading_links do %>
        <%= heading_links %>
      <% end %>
    <% end %>
    <%= panel.with_body do %>
      <%= @description.notes.tpl %>
    <% end %>
  <% end %>

<% end %>

