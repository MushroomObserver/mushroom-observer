<%
add_page_title(:edit_location_title.t(name: @location.display_name))
add_pager_for(@image)
add_context_nav(location_form_edit_tabs(location: @location))

set_container(:full)
%>

<%= render(partial: "locations/form", locals: { local: true }) %>
