<%
desc_title = description_title(@user, @description)
add_page_title(:show_past_location_description_title.t(
  num: @description.version, name: desc_title
))
add_context_nav(location_description_version_tabs(description: @description,
                                              desc_title: desc_title))
%>

<%= render(
      "controllers/shared/version_table",
      obj: @description,
      versions: @versions
    ) %>

<%= render(partial: "descriptions/description_details_and_alts_panel") %>

<%= render Components::ObjectFooter.new(user: @user, obj: @description, versions: @versions) %>
