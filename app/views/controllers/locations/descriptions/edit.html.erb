<%
add_page_title(
  :edit_location_description_title.t(name: @description.format_name)
)

add_context_nav(location_description_form_edit_tabs(description: @description))
%>

<%= render(partial: "locations/descriptions/form",
           locals: { action: :update }) %>
