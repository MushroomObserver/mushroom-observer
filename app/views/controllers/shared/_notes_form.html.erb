<%# Notes section of a form %>

<!-- NOTES -->
<div id="notes">

  <p>
    <span class="font-weight-bold"> <%= :NOTES.t %>:</span>
  </p>
  <div class="ml-5">
  <% fields.each do |field| %>
    <%= form.fields_for(:notes) do |f_n| %>
      <%= text_area_with_label(
            form: f_n, rows: 1, label: field.label + ": ",
            field: field.name,
            value: field.value) %>
    <% end # form.fields_for(:notes) %>
  <% end # each do field %>
  </div>

</div><!--#notes-->
<!-- /NOTES -->
