<%
add_page_title(:ask_webmaster_title.t)
%>

<%= render(Views::Controllers::Admin::Emails::WebmasterQuestions::Form.new(
      reply_to: @email,
      message: @message,
      email_error: @email_error,
      local: true
    )) %>
