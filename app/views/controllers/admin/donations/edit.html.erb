<%
container_class(:full)
add_page_title(:review_donations_title.l)
add_context_nav(Tab::Admin::DonationsFormEdit.new)
%>

<%= render(Views::Controllers::Admin::Donations::ReviewForm.new(
      FormObject::ReviewDonations.new,
      donations: @donations
    )) %>
