<%
  container_class(:wide)
  add_page_title(:index_license_header.t)
  add_context_nav(Tab::License::IndexActions.new)

  flash_error(@error) if @error && @objects.empty?
%>

<div>
  <%=
    make_table(
      headers: license_table_header(),
      rows: license_table_rows()
    )
  %>
</div>
