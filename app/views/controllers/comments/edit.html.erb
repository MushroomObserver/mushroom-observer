<%
add_page_title(comment_form_edit_title(target: @target))
add_context_nav(comment_form_edit_tabs(comment: @comment))
%>

<%= render(partial: "comments/form",
           locals: { action: :update, local: true }) %>
<%= render(partial: "comments/object", object: @target) %>
