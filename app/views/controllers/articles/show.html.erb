<%
container_class(:wide)
add_show_title(@article.display_title.t, @article)
add_edit_icons(@article)
%>

<%= content_padded do %>
  <%= :BY.t %> <%= user_link(@article.user) %>,
  <%= tag.small(@article.created_at.display_time) %>
<% end %>

<%= render(Components::Panel.new(panel_class: "mt-3")) do |panel| %>
  <%= panel.with_body { @article.body.tpl } %>
<% end %>

<%= show_object_footer(@user, @article) %>
