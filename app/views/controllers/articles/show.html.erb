<%
container_class(:wide)
add_page_title(article_show_title(@article),
               "#{:ARTICLE.l}: #{@article.display_title.t}")
add_context_nav(article_show_tabs(article: @article, user: @user))
%>

<%= content_padded do %>
  <%= :BY.t %> <%= user_link(@article.user) %>,
  <%= tag.small(@article.created_at.display_time) %>
<% end %>

<%= panel_block(class: "mt-3") do @article.body.tpl end %>

<%= show_object_footer(@user, @article) %>
