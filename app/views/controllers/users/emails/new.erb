<% add_page_title(:ask_user_question_title.t(user: @target.legal_name)) %>

<%= render(Components::UserQuestionForm.new(
      FormObject::UserQuestion.new,
      target: @target,
      local: true
    )) %>
