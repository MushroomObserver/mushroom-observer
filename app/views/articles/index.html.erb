<%
  @title ||= :ARTICLES.t
  tabs = create_links(index_links_for_user(@user))
  @tabsets = { right: draw_tab_set(tabs) }
  @container = :wide

  flash_error(@error) if @error && @objects.empty?
%>

<%= paginate_block(@pages) do %>
  <div class="list-group">
    <%= render(partial: "object", collection: @objects) %>
  </div>
<% end %>
