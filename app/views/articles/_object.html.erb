<div class="list-group-item">
  <%= content_tag(:span, class: "text-lg", link_with_query(object.title.t,
                  object.show_link_args)) %>
  <br>
  <%= content_tag(:span, class: "small", object.created_at.web_time) %>:
  <%= user_link(object.user) %>
  <br>
  <%= truncate(strip_tags(object.body), length: 80).tpl %>
</div>
