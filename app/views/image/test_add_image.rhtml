<% @title = 'Test Upload Speed' %>

This page is strictly for testing image upload performance.  Images uploaded
through this page are not saved and no database changes are made.<br/><br/>

<% if @log_entry %>
  Test <%= @log_entry.id %> created: <%= @log_entry.created %><br/>
<% end %>

<% form_tag({:action => 'test_upload_image', :log_id => @log_entry.id}, :multipart => true) do %>
  <center><%= submit_tag("Upload") %></center>

  <p>
    Image 1: <%= file_field("upload", "image1") %><br/>
    Image 2: <%= file_field("upload", "image2") %><br/>
    Image 3: <%= file_field("upload", "image3") %><br/>
    Image 4: <%= file_field("upload", "image4") %><br/>
  </p>

<% end %>
