<%
  @title = :image_reuse_title.t(name: @object.unique_format_name)
  @tabsets = {
    right: render(partial: "shared/tab_set", locals: {
      links: [
        link_with_query(:show_object.t(type: @object.class),
                        controller: @object.show_controller, action: @object.show_action, id: @object.id),
        link_with_query(:edit_object.t(type: @object.class),
                        controller: @object.show_controller, action: @object.edit_action, id: @object.id)
      ]
    })
  }
  url_args = {action: :reuse_image_for_glossary_term, id: @object.id}
%>

<%= render(partial: "reuse_image", locals: {url_args: url_args}) %>
