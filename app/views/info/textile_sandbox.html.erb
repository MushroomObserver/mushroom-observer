<%
add_page_title(:sandbox_title.t)
%>

<%= help_block(:div, :sandbox_header.tp) %>

<% if !@code.nil? %>
  <strong><%= :sandbox_look_like.t %>:</strong>
  <div class="sandbox">
    <% if @submit == :sandbox_test.l %>
      <%= @code.tpl %>
    <% else %>
      <code>
        <%= escape_html(@code.tpl) %>
      </code>
    <% end %>
  </div>
<% end %>

<%= form_with(url: { action: :textile_sandbox }) do |f| %>

  <% if !@code.nil? %>
    <div class="sandbox-up-ptr mx-auto mt-3 mb-3">
      <%= image_tag("up_arrow.png") %>
      <%= f.submit(:sandbox_test.l, class: "btn btn-outline-secondary") %>
      <%= f.submit(:sandbox_test_codes.l, class: "btn btn-outline-secondary") %>
      <%= image_tag("up_arrow.png") %>
    </div>
  <% end %>
  <%= text_area_with_label(form: f, field: :code, value: @code, rows: 8,
                           label: :sandbox_enter.t + ":") %>
  <% if @code.nil? %>
    <%= submit_button(form: f, button: :sandbox_test.l, center: true) %>
  <% end %>

<% end %>

<div class="mt-3">
  <p><strong><%= :sandbox_quick_ref.t %>:</strong></p>
  <pre><%= :sandbox_sample.l %></pre>
</div>

<strong><%= :sandbox_more_help.t %>:</strong>
<div class="pl-3">
  <%# translation not needed as document title is static %>
  <%= link_to(
        "MO Flavored Textile", "https://docs.google.com/document/d/10NiaPDKoK_k3bRIoU1smGXSycDczf_jdkmW-xt-Wf20/edit?usp=sharing"
      ) %><br/>
</div>

<strong><%= :sandbox_web_refs.t %>:</strong>
<div class="pl-3">
  <%= link_to(:sandbox_link_hobix_textile_reference.t,
              "https://hobix.com/textile") %><br/>
  <%= link_to(:sandbox_link_hobix_textile_cheatsheet.t,
              "https://hobix.com/quick") %><br/>
  <%= link_to(:sandbox_link_textile_language_website.t,
              "https://textile-lang.com/") %><br/>
</div>
