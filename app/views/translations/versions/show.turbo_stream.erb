// MOTranslations.show_whirly(MOTranslations.LOADING_STRING);

// MOTranslations.hide_whirly();
// This is for very long translations, where the versions would be way long
// Still maybe nicer inline, scrolling? Idk.
alert('<%= @versions %>');
// stimulus needs to print the alert with an id, then turbo will fill the id.
