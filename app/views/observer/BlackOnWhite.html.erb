<%
  @title = :theme_black_on_white.tl

  tabs = [
    link_to(:theme_list.t,
            :controller => 'observer', :action => 'color_themes'),
    link_to(:app_preferences.t,
            :controller => 'account', :action => 'prefs')
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<%= :theme_black_on_white_description.tp(:link => link_to('**__Amanita__**'.t, :action => "Amanita")) %>

<%= :theme_switch.tp(:theme => :BlackOnWhite.t) %>
