xml.results(:number => @objects.length) do
  for comment in @objects
    render_xml_partial(xml, :comment, :locals => {
      :comment => comment,
      :details => true,
      :refs => false,
    })
  end
end
