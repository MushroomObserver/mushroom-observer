xml.results(:number => @objects.length) do
  for obs in @objects
    render_xml_partial(xml, :observation, :locals => {
      :observation => obs,
      :details => true,
      :refs => true,
    })
  end
end
