// What to do on ajax update. Eventually needs to handle other @back_object

<%= render(partial: "collection_numbers/update_observation", format: :js) %>
<%= render(partial: "collection_numbers/update_flash", format: :js) %>
