// Collection numbers may have flash warnings that still update the coll. num.
$("#flash_notices").replaceWith("<%= j render(partial: 'layouts/app/flash_notices', locals: { id: 'flash_notices' }) if flash_notices? %>");

// Finally, remove the modal
$("#modal_collection_number").modal('hide');
