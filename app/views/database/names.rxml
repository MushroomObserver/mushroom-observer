xml.query @query
xml.number @number
xml.pages @pages
xml.names do
  xml.number @objects.length
  for name in @objects
    render_xml_partial(xml, :name, :locals => {
      :name => name,
      :details => true,
      :refs => true,
    })
  end
end
