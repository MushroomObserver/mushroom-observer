if @objects.length > 0
  xml.comments do
    xml.number @objects.length
    for comment in @objects
      render_xml_partial(xml, :comment, :locals => {
        :comment => comment,
        :details => true,
        :refs => true,
      })
    end
  end
end
