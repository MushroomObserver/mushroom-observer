if @objects.length > 0
  xml.images do
    xml.number @objects.length
    for image in @objects
      render_xml_partial(xml, :image, :locals => {
        :image => image,
        :details => true,
        :refs => true,
        :files => true,
      })
    end
  end
end
