xml.results(:number => @objects.length) do
  for image in @objects
    render_xml_partial(xml, :image, :locals => {
      :image => image,
      :details => true,
      :refs => false,
      :files => true,
    })
  end
end
