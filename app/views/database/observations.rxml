if @objects.length > 0
  xml.observations do
    xml.number @objects.length
    for obs in @objects
      render_xml_partial(xml, :observation, :locals => {
        :observation => obs,
        :details => true,
        :refs => true,
      })
    end
  end
end
