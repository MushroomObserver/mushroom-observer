<%
  @title = :edit_herbarium_title.l

  links = create_links(herbarium_form_edit_tabs(@herbarium))
  @tabsets = { right: draw_tab_set(links) }
%>

<%= render(partial: "form", locals: { button_name: :SAVE }) %>
