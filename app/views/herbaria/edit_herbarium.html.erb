<%
  @title = :edit_herbarium_title.l

  tabs = [
    link_with_query(:cancel_and_show.t(type: :herbarium), @herbarium.show_link_args),
    link_with_query(:herbarium_index.t, action: :index)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<div class="max-width-text">
  <%= form_tag(add_query_param(action: :edit, id: @herbarium,
                               back: @back)) do %>
    <%= render(partial: "form", locals: {button: :SAVE}) %>
  <% end %>
</div>
