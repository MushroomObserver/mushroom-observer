<%
add_page_title(
  :create_location_description_title.t(name: @location.display_name)
)
add_tab_set(location_description_form_new_tabs(description: @description))
%>

<%= render(partial: "locations/descriptions/form",
           locals: { action: :create }) %>
