<%
  tabs = [
    link_with_query(:cancel_and_show.t(type: :comment),
                    @comment.show_link_args)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<%= render(partial: "form", locals: { action: :update, local: true }) %>
<%= render(partial: "object", object: @target) %>
