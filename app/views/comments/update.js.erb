// What to do on ajax update. No flash here, so it just hides/removes modal

<%= render(partial: "comments/update_comments_for_object", format: :js) %>

// Finally, remove the modal
$("#modal_comment").attr('data-busy', 'false').modal('hide');
