// What to do if create or update doesn't save

var modal_body = $("#modal_body_comment");

// replace the flash notices in the modal
$("#modal_flash").remove();
modal_body.prepend("<%= j render(partial: 'layouts/app/flash_notices', locals: { id: 'modal_flash' }) if flash_notices? %>");

// replace the form, should show
$("#comment_form").remove();
modal_body.append('<%= j render(partial: "comments/form", locals: { action: @action, local: false }) %>')
