<%
@title = :edit_publication_title.l

tabs = create_tabs(publication_form_edit_links(pub: @publication))
@tabsets = { right: draw_tab_set(tabs) }
%>

<%= render(partial: "form", locals: { action: :update }) %>
