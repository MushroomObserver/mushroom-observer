<%
@title = :create_publication_title.l

tabs = create_links(publication_form_links(pub: @publication))
@tabsets = { right: draw_tab_set(tabs) }
%>

<%= render(partial: "form", locals: { action: :create }) %>
