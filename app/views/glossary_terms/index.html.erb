<%
@title = :glossary_term_index_title.t

tabs = User.current ? create_tabs(glossary_term_index_links) : []
@tabsets = { right: draw_tab_set(tabs) }
@container = :text_image
%>

<%=
  tag.div(
    link_to(:glossary_term_index_documentation.tp,
    "https://github.com/MushroomObserver/mushroom-observer/blob/main/doc/glossary.md"),
    class: "container-text"
  )
%>
<%= paginate_block(@pages) do %>
  <div class="list-group">
    <%= render(partial: "object", collection: @objects) %>
  </div>
<% end %>
