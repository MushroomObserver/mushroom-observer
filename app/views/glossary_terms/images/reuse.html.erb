<%
# Note. Move to glossary_terms/images/new
#       GlossaryTerms::ReuseImagesController#new
# Move partial to shared/reuse_image

@title = :image_reuse_title.t(name: @object.unique_format_name)

tabs = [
  link_with_query(:show_object.t(type: @object.type_tag),
                  @object.show_link_args),
  link_with_query(:edit_object.t(type: @object.type_tag),
                  @object.edit_link_args)
]
@tabsets = { right: draw_tab_set(tabs) }
@container = :full

form_action = { controller: "/glossary_terms/images",
                action: :attach,
                id: @object.id,
                q: get_query_param }
%>

<%= render(partial: "shared/images_to_reuse",
           locals: { form_action: form_action }) %>
