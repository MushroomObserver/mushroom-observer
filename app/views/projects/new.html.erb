<%
@title ||= :add_project_title.t

tabs = create_tabs(project_form_new_links)
@tabsets = { right: draw_tab_set(tabs) }

button = :CREATE.l
%>

<%= render(partial: "form", locals: { button: button }) %>
