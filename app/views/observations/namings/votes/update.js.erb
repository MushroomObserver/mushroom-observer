// This handles successful change of vote:
// - via a `select` in the namings table on show obs,
// - or the Observations Needing Id index page
// Not bothering with context param for this, but if implemented
// the below logic would be handled in the controller as in namings

// Are we on show_obs, or obs_needing_ids?
if ($("#observation_namings").length) {
  <%= render(partial: "observations/namings/update_observation") %>
} else {
  // update matrix box
  <%= render(partial: "observations/namings/update_lightbox") %>
  $('#mo_ajax_progress_caption').empty();
  $('#mo_ajax_progress').modal('hide');
}
