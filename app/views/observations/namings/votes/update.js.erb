// This handles successful change of vote via a `select` in the namings table
// or the Observations Needing Id page

// Are we on show_obs, or obs_needing_ids?
if ($("#observation_namings").length) {
  <%= render(partial: "observations/namings/update_observation", format: :js) %>
} else {
  // update matrix box
  $('#mo_ajax_progress_caption').empty();
  $('#mo_ajax_progress').modal('hide');
}
