// What to do on ajax create

// Are we on show_obs, or obs_needing_ids?
if ($("#observation_namings").length) {
  <%= render(
    partial: "observations/namings/update_observation",
    format: :js
  ) %>
} else {
  <%= render(
    partial: "observations/namings/update_lightbox",
    format: :js
  ) %>
}
