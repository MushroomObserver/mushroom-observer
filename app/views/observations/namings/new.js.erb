$('body').append('<%= j render(partial: "observations/namings/modal") %>');

$('#modal_propose_naming').on('shown.bs.modal', function() {
  new MOAutocompleter({ input_id: "naming_name" });
  $('#naming_name').focus();
  // alert(JSON.stringify(AUTOCOMPLETERS));
  console.log("should be completing");
  // new MOAutocompleter(jQuery.extend({
  //   input_elem: new_elem,
  //   primer: primer.join("\n"),
  //   pulldown_size: length,
  //   act_like_select: true
  // }, opts))
});

$('#modal_propose_naming').modal('show');

$('#modal_propose_naming').on('hidden.bs.modal', function () {
  $(this).remove();
});
