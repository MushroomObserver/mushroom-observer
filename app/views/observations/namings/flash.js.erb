// What to do if create doesn't save

var modal_body = $("#modal_propose_naming_body");

// replace the form, should show
$("#naming_form").remove();
modal_body.append('<%= j render(partial: "observations/namings/form", locals: { action: :create, show_reasons: true, local: true }) %>')
