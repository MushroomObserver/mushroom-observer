<%
# The form partial is also called by species_lists/downloads
@title = :download_observations_title.t

# tabs = create_tabs(observation_download_links)
# @tabsets = { right: draw_tab_set(tabs) }
%>

<%= render(partial: "observations/downloads/form",
           locals: { query_param: get_query_param }) %>

