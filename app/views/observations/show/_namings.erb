<%
@query ||= nil
%>

<!--OBSERVATION_NAMINGS / TABLE LAZY LOADED AND REFRESHED BY TURBO -->
<%= tag.turbo_frame(
  id: "observation_namings",
  src: observation_namings_path(observation_id: obs.id),
  loading: "lazy"
) do

  render(partial: "observations/namings/table",
         locals: { obs: obs, query: @query })
end %>
<!--/OBSERVATION_NAMINGS -->
