<%
  @title ||= @description.format_name.t

  tabs = [
    link_with_query(:show_object.t(type: :location),
                    @description.location.show_link_args),
    link_with_query(:show_object.t(type: :location_description),
                    @description.show_link_args)
  ]
  @tabsets = { right: draw_tab_set(tabs) }
%>

<div class="row">
  <div class="col-xs-12 max-width-text">
    <%= render(partial: "shared/adjust_permissions",
               locals: { description: @description, groups: @groups,
                         data: @data }) %>
  </div>
</div>
