<%
  @title ||= :merge_descriptions_title.t(:object => @description.format_name)

  new_tab_set do
    add_tab_with_query(:show_object.t(:type => :location),
      :action => 'show_location', :id => @description.parent_id)
    add_tab_with_query(:show_object.t(:type => :location_description),
      :action => 'show_location_description', :id => @description.id)
  end
%>

<%= render(:partial => 'shared/merge_descriptions', :locals =>
           { :description => @description }) %>
